meta {
  name: 02 Get current user
  type: http
  seq: 2
}

get {
  url: {{gateway}}/api/auth/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 200
  res.body.email: eq {{email}}
}
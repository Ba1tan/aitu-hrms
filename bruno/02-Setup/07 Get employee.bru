meta {
  name: 07 Get employee
  type: http
  seq: 7
}

get {
  url: {{gateway}}/api/v1/employees/{{employeeId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 200
  res.body.id: eq {{employeeId}}
}